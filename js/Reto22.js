//Definimos nuestra funcion para sumar los cubos
const sumaDeCubos = (num1, num2, num3) => Math.pow(num1, 3) + Math.pow(num2, 3) + Math.pow(num3, 3)

//Mostramos los datos
console.log(sumaDeCubos(1,2, 3));